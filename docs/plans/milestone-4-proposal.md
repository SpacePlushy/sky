# Milestone 4 proposal: Alerts and polish

**Status:** Built 2026-09-25 without the owner's approval, for the same reason as Milestones 2 and 3.
This plan had no separate review before the build; the built code had an adversarial multi-agent
review, and what it changed is recorded at the end.
**Date:** 2026-09-25.

## Goal

Make the dashboard tell you when to go outside, prove it works end to end in a real browser, and
present the project to someone who has five minutes.

### Done when

- Visible passes can raise a browser notification a chosen number of minutes before they start,
  and can be exported as a calendar file with an alarm on each visible pass (kept by Apple Calendar
  and Outlook; Google Calendar uses its own reminders for imported events).
- Playwright tests drive the built dashboard against the API, offline, in CI.
- The README opens with what the project is, a screenshot, how it is verified, and how to run it.

## Decisions

| # | Decision | Choice | Main alternative |
|---|---|---|---|
| D1 | Alerts | Two kinds, both opt-in: browser notifications while the dashboard is open, and an iCalendar (RFC 5545) export of visible passes with alarms | A server that pushes messages (email, ntfy, web push): needs accounts, keys, or a running service, against "no secrets" and a local tool |
| D2 | What alerts cover | Visible passes only by default, with a toggle for all passes | Everything |
| D3 | Calendar times | UTC (`Z`) in the file; calendar apps show them in the device's zone | Floating local times, which break for anyone in another zone |
| D4 | End-to-end setup | Playwright starts the published API offline with the demo cache and a clock started at 2026-09-24 04:00 UTC, serving the built dashboard; Chromium on desktop and phone viewports | Mocking the API in the browser, which would not test the real contract |
| D5 | Screenshots | Generated by a Playwright script from the same offline setup and committed under `docs/images/`, after checking `/api/config` shows the public Capitol location | Hand-made screenshots, which drift and could show a private location |

## Alerts

- **Browser notifications.** A control asks for permission only when clicked. The lead time
  is 5, 10, or 15 minutes, stored in the browser. The page schedules a notification for each
  upcoming visible pass from the pass list and the server clock, reschedules when the passes
  refresh, and never notifies twice for one pass. They work only while a dashboard tab is open,
  and the control says so.
- **Calendar export.** `GET /api/satellites/{id}/passes.ics?days=7&visibleOnly=true` returns one
  event per visible part: start and end in UTC, a summary such as "ISS visible, up to 36°", a
  description with rise, peak, and set directions, and a display alarm 10 minutes before. Event
  UIDs come from the satellite and the pass's rise time, so importing a later file updates events
  instead of duplicating them. Lines are folded at 75 octets and end in CRLF, as RFC 5545 requires.

## End-to-end tests

Against the demo data at the fixed clock, where the first passes are known from the Skyfield
reference:

- The page loads, shows the offline and simulated-clock notice, and the telemetry values match
  `/api/satellites/25544/now` at the same instant.
- The pass table lists the same passes as `/api/.../passes`, with the visible ones marked in text.
- Selecting a pass draws it on the sky plot and keeps the selection when the list refreshes.
- Times show in America/Phoenix whatever the browser's own zone: the test runs the browser in
  another zone.
- At 375 px wide nothing scrolls sideways, and the pass list is readable.
- Turning on notifications asks for permission, and a pass inside the lead time produces exactly
  one notification (the browser's clock is driven by Playwright).
- The calendar link downloads a file that parses, with one event per visible part.
- No request leaves the machine: the test fails on any request to a host other than the API.

## README

For a hiring manager: a one-paragraph summary, a screenshot of the dashboard, the verification
story in a short table (what is checked, against what, the worst case), an architecture diagram,
how to run it in one command, what was hard, and the milestones. "Why I built this" stays for the
owner to write.

## Build order

1. Calendar export in the API, with tests (RFC 5545 structure, UTC times, stable UIDs, folding).
2. Notifications in the dashboard, with unit tests for scheduling.
3. Playwright setup, the end-to-end tests, and a CI job.
4. Screenshots and the README.
5. Docs: verification record, handoff guide.

## Out of scope

- Notifications when no dashboard tab is open (web push needs keys and a service).
- Accounts, hosting beyond the local machine, and brightness predictions.

## Code review

An adversarial review of the built milestone (calendar, alerts, and tests-and-docs lenses, each
finding checked by a skeptic) confirmed 17 findings. They changed the build:

- **Calendar UIDs** came from the minute a pass rises, so new elements moving a rise across a
  minute boundary duplicated events on re-import. They now come from the revolution number at the
  culmination (REV_AT_EPOCH plus node crossings), which new elements do not change.
- **An empty export** is a 404 problem, not a calendar with no events, which RFC 5545 forbids; the
  dashboard shows the link only when there is a visible pass to export.
- **Event descriptions** round times as DTSTART does, and carry the epoch and any element-age or
  SGP4 warning, since the file outlives the page.
- **"Alarms work on a phone"** was not true for Google Calendar, which ignores imported alarms; the
  README, the dashboard, and the code say which apps keep them.
- **Alerts across tabs**: a Web Lock serializes the check, so two tabs cannot both notify; a
  preference changed in one tab reaches the others; a restarted simulated clock no longer silences
  replayed passes; a browser whose notification constructor throws (Chrome for Android) shows the
  control as unavailable instead of on; the toggle keeps focus while the permission prompt is open.
- **End-to-end tests** pick passes by rise time, not position, so the pass ending a minute after the
  demo clock starts cannot shift them.

Two limits remain, stated rather than hidden. Chromium passes storage changes between tabs
asynchronously, so the lock makes a duplicate alert very unlikely rather than impossible; the
shared tag then replaces the first notification rather than adding a second. And after a restart of
the simulated clock, a replayed pass alerts again only if a tab checks within about 26 seconds of
the restart; later, a stale entry cannot be told apart from a real one.
